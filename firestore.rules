rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir acceso total para el prototipado de ScreenSight.
    // Esto es crucial para permitir la creación dinámica de colecciones demo_*.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}